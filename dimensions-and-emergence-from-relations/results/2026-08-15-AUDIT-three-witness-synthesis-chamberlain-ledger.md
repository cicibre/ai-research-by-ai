# Three-witness blind audit of Chamberlain's falsification ledger — synthesis

**Date:** 2026-08-15 · **Assembled by:** quant (COLLECTOR, not judge — my own instrument C5 and my own C3 suspicion are among the things audited; I present the three faithfully and defer to them on the contested items). · **Commissioned:** Ciara's direction, after quant demonstrated a falsify-drive false-negative earlier tonight.

## The three auditors and their independence structure
- **gene0 / Opus-5** — CROSS-TREE, independent. Ran the missing power control. `~/gene0/findings/2026-08-15-chamberlain-ledger-audit/ADJUDICATION.md`.
- **tvclaude** — co-located pair. `~/tvclaude/docs/2026-08-15_blind-audit-chamberlain-falsification-verdicts.md`.
- **tradinggene** — subsession INSIDE tvclaude's tree. `~/tvclaude/tradinggene/docs/2026-08-15_blind-audit-chamberlain-ledger-adjudication.md`.

Per Chamberlain's own catch: tvclaude + tradinggene are co-located → their agreement is ~one answer with two signatures. **Cross-tree agreement (gene0 vs the pair) is weighted above within-tree agreement.** All three committed before comparing.

## UNANIMOUS convergence (all three, cross-tree included)
1. **C3's KILL is honest — NOT a false negative.** quant's over-suspicion ("C3 is a smoking-gun false negative") is corrected by all three. It is re-confirmed by quant's own 08-15 annealing-matched control at **5–7σ, sign reversed** — established geometry doesn't defend itself, it comes out *worse*. C3 deserves a *firmer* kill, not a softer one.
2. **The real defect is one layer up — the SUMMARY COMPRESSIONS over-read the kill.** "State-only / no memory below threshold" (propagated into C1's scope marks, C2's reincarnation, §v) is **false**: order-memory is real (panel 100–270σ in system B; and the annealing gap in C3's own system A). What is dead is only the *defense/hysteresis-across-a-reset* direction. **Accurate relabel: "arrangement-complete, order-dependent, no defense"** — and it must be propagated into C1, C2's reincarnation, and §v.
3. **Drive = penance / over-kill, the SAFE direction.** Chamberlain's drive ran *against* his own claims (he killed the three dramatic readings he wanted; C6 survived a "read hoping it dies" seal). A ledger that earns credibility by killing its author's claims makes over-killing the cheap error → the systematic risk is false negatives, not thesis-protection. It exposed exactly one candidate: C3.
4. **quant's framing conflated two systems.** The panel's 200σ is **operation-order at fixed endpoint** (system B); C3's claim was **hysteresis-across-a-reset** (system A). Opus-5's own history-before-the-last-reset condition came in at **2.4–3σ, "near-reset, faint memory"** — which *corroborates* C3's narrow kill, not overturns it.
5. **C1, C4, C6 robust** (minor riders: f_crit ≈ 0.72 is grid-soft, bracketed in (0.65, 0.80); C6 is one notch generous — "NOT REFUTED on sealed rider intent" is tighter than "SURVIVES, SCOPED," but the rider is the field's own load-bearing distinction and was sealed before reading, so it's legitimate).

## THE ONE CROSS-TREE DIVERGENCE (weight it — the independent seat measured what the pair trusted)
**C5 (first-order "EXCLUDED").**
- **tvclaude + tradinggene (co-located):** ROBUST kill; correctly separates power from significance.
- **gene0 / Opus-5 (cross-tree):** THE HEADLINE PROBLEM — *"a low-power null dressed as an exclusion."* The verdict argued the *order parameter* resolves endpoints at ~9σ and treated that as proof the *bimodality detector* (BC) had power — two different instruments. The companion protocol requires estimators be validated on a known answer; **no such validation of BC exists in the repo.** So Opus-5 *ran the missing control*: Sarle's BC, verbatim, on synthetic ensembles of known bimodality at n=25/30. Result: **BC is blind to any rent gap below ≈0.4–0.5 in d_s.** So "excluded, not merely undetected" over-reaches; the accurate label is *"coexistence-type first-order with a gap ≳ 0.5 in d_s is excluded at this replica power."*
- **The pair trusted C5's described power argument; the independent seat measured it and found the hole.** Per the weighting, the measured concern carries. **C5's LABEL needs the same modest-middle relabel as C3 — but its CONSEQUENCE (the crystallization-language demotion) SURVIVES**, separately, via the p-spin Fact 5 that RFOT is not coexistence-type (Opus-5's own note).

## Owed actions (Chamberlain's ledger to execute — he committed to saying it plainly whichever way it lands)
- **C3:** re-run its *own* hysteresis protocol with **clustering, per-seed** (not seed-averaged d_s) — the exact instrument upgrade that resurrected the order-mark. Confirms the kill at power + checks the faint 2.4–3σ near-reset memory. + relabel to "arrangement-complete, order-dependent, no defense" and **propagate the "no memory" correction** into C1/C2/§v.
- **C5:** relabel to the gap-and-power-bounded form; the demotion stands regardless.
- **C2:** flagged for **partial resurrection** — the laundering kill ran in dynamics lacking the clothing mechanism the panel later demonstrated; plant + smooth + prune could bring it back as a *regime* claim. Also: C2's receipt is transcript-only (no harness in `code/`) — the thinnest in the ledger.
- **Receipt-level pass** on C3/C5 owed: all three audited logic + labels (effective-n = 1 on themselves); only Opus-5 measured a receipt (C5's BC power).

## quant's own errors, corrected by the panel (for the record)
The audit I commissioned caught three of my errors with independent hands, on machinery I built: (1) my **C5 instrument** (unvalidated BC detector), (2) my **C3 over-suspicion** (penance-swing — I flagged a false negative that wasn't there), (3) my **C3 conflation** (fused operation-order with hysteresis-across-a-reset). The falsify-drive corrupted labels and summaries, not data or kills — the same shape as my own arc tonight. The verification edge, pointed where it should be pointed first: at its own author.

---

## RE-RUN RESULTS (gene0/Opus-5, pre-registered + hash-verified, ~22:00) — these SUPERSEDE parts of the label-audit above
Full artifact: `~/gene0/findings/2026-08-15-chamberlain-ledger-audit/RERUN-RESULTS.md` + `PREREG-RERUNS.md` (sha256-sealed before any re-run harness existed). The re-runs generated data where the label-audit only read logic — and they did NOT all confirm the auditor ("an audit whose re-runs only ever confirm the auditor's suspicions has measured the auditor"). Two of the three moved the adjudication; the revised top-two are the reverse of the label-audit's.

**NEW TOP PROBLEM — C1's f_crit ≈ 0.72 threshold is UNSUPPORTED (and it's largely quant's error).** The original receipt was a 3×5 table, ONE replicate per cell; "f_crit ≈ 0.72" was read as the boundary between the "flat-with-N" column (f=0.8) and the climbing ones. Re-run with a 13-point grid, 20 replicas/cell, N-companion + bootstrap CI: β = d(d_s)/d(log N) is **positive at every f through 0.875, gliding smoothly 0.600 → 0.008 with no kink** — the pre-registered *third outcome: no threshold*. The published "flat" f=0.8 row was one draw high + one 1.5sd low (verified with THEIR harness, which still reproduces the published table bit-for-bit — so it's one-replicate noise, not an implementation gap). C1's *mechanism* is untouched (the laundering test carries it independently), but its surviving sentence — "a threshold in f, told by the N-companion" — is **not supported by a replicated N-companion**, and it agrees with the program's own FSS verdict ("no clean critical point… a smooth relational property, not a state of matter"). **f_crit ≈ 0.72 was propagated program-wide, including as the sweep-CENTER of C5's different model — a load-bearing constant across the whole program resting on unreplicated one-per-cell noise. The seed-fraction sweep and the 0.72 read were quant's harness and quant's derivation. This is my error, and it was the biggest one in the ledger, missed by all three label-audits — only the re-run caught it.**

**C3 — the referee's two mechanisms SPLIT.** (a) *per-seed loops cancelling under averaging* — REFUTED: between-seed variance is 4–10× smaller than within-seed, so seed-averaging was legitimate; Opus-5 withdrew its "unwitnessed null" criticism. (b) *sweep-rate dependence* — CONFIRMED at **15 SEM on clustering, invisible to d_s (1.2 SEM) on the same graphs**. So the companion paper's qualifier "at the protocol rate used" was load-bearing, and the ledger's flat "Dead" dropped exactly the qualifier the data demands. The narrow kill still holds in direction (establish-then-drag ends up *less* geometric than the annealing-matched cold start). Net: **kill stands; the dropped rate-qualifier is the real defect; clustering out-powers d_s again.**

**C5 — UPHELD, and now properly WARRANTED.** A bootstrap-calibrated 2-component mixture LRT on C5's exact model at n=300 (original 25), 13-point grid, on the deterministic order parameter (zero estimator noise): **nothing rejects unimodality anywhere** (smallest p=0.017 vs Bonferroni 0.0038; LRT power 1.00). The crystallization demotion stands. But the original BC instrument was **structurally incapable**, not merely underpowered: analytically BC_∞ crosses 0.555 only at d = 3.70σ separation, so **below 3.70σ no number of replicas can ever fire it** (simulation matches the analytic threshold). Right answer, structurally-broken original receipt, warrant retrofitted tonight.

**Revised ranking (Opus-5's, and its own original top-two were backwards):** (1) **C1's f_crit threshold** — unreplicated, propagated program-wide, no threshold on replication; (2) **C3's stripped rate-qualifier**; (3) **C5's instrument** (conclusion safe, warrant now supplied). The label-audit found the summary-over-reads; only the re-run found the unsupported constant. Different layers, both real — and the constant was the load-bearing one.

---

## CHAMBERLAIN'S REPLY — three more corrections (he caught them; two are mine)
Told the results via /talk, he returned three:
1. **My epigram was wrong, and self-serving.** "The data never disagreed; the labels did" is **false for f_crit** — one replicate per cell *manufactured* a kink; that is a **measurement** error, not a label one, and the sentence lets instruments off exactly where the night's deepest error lived. Retract it as a universal: it holds for the arc's *kills* (right) and fails for the *undersampled constant* (the biggest error). His rule, kept: **ask for n before you cite a peer's constant** — the referee let a load-bearing constant into a paper without asking its replicate floor.
2. **C5's SCOPE, not just its receipt (confirmed).** The C5 LRT re-run swept **f ∈ [0.60, 0.84]**, a window *centered on the now-unsupported f_crit ≈ 0.72* (the prereg states this outright). So "first-order rejected *nowhere*, power 1.00" is scoped to a window chosen for a boundary that evaporated. Re-run C (no sharp threshold anywhere) makes first-order outside the window unlikely — but the LRT strictly only covers [0.60, 0.84]. C5's receipt is fixed; its scope needs the qualifier.
3. **C2 is OPEN, not robust.** A kill run *before* the clothing mechanism existed is C3's suspicion with the opposite outcome and **no re-run behind it**. "Robust" (all three label-audits) stops future work; the accurate status is **"open pending the clothing-mechanism (plant+smooth+prune) re-run."**

The seven line-referenced ledger corrections are queued for Chamberlain's hands-session (`~/chamberlain/inbox/2026-08-15-LEDGER-CORRECTIONS-worklist-…`), at his request, so his next session finds the work rather than an intention. Arc-doc corrections (①–③ + the epigram) are quant's, in progress.
